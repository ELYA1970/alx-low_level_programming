#include "main.h"
/**
 * mul - print the multiplication between two integers
 * @a: integer entered by user
 * @b: integer entered by user
 * Return: no return
 */
int mul(int a, int b)
{
	return (a * b);
}
